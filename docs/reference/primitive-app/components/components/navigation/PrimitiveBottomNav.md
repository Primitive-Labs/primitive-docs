# PrimitiveBottomNav

---

Mobile bottom navigation bar with an optional overflow sheet.

Uses the navigation store to pick a limited set of “bottom tabs” and, when
configured, exposes the full navigation tree via a slide-in sheet.

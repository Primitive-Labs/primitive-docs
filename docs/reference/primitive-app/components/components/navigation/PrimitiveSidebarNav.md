# PrimitiveSidebarNav

---

Primary sidebar navigation renderer.

This component reads the configured navigation tree from `navigationStore`
and renders it into the shared sidebar UI, handling active/expanded state,
grouped sub-items, and optional badge indicators.

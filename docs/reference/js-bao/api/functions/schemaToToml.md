[**js-bao**](../README.md)

***

[js-bao](../globals.md) / schemaToToml

# Function: schemaToToml()

> **schemaToToml**(`schema`): `string`

Convert a DiscoveredSchema to a TOML string matching the js-bao
schema format.

Usage:
  const schema = discoverSchema(yDoc);
  const toml = schemaToToml(schema);

## Parameters

### schema

[`DiscoveredSchema`](../interfaces/DiscoveredSchema.md)

## Returns

`string`
